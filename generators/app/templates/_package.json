{
  "name": "<%= app %>",
  "version": "0.1.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "prestart": "tsc",
    "start": "node .",
    "postinstall": "typings install dt~node --global --save",
    "typings":"typings"
  },
  "devDependencies": {
    "typings": "^2.1.0"
  }
}